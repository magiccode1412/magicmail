#!/bin/bash

### This script is called after the user change environment variables in application setting page.
### 已移除配置向导（wizard/config）：单入口（统一网关）模式下无运行时可配置项，无需重启。
### 保留本脚本为空实现，避免平台在缺失该回调时报错。

exit 0
