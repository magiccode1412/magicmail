{
    "name": "magicmail-docs",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "vitepress dev",
        "build": "vitepress build",
        "preview": "vitepress preview",
        "deploy": "export VITEPRESS_BASE=/magicmail/ && vitepress build && cd .vitepress/dist && git add . && git commit -m 'deploy: update site static files' && git push origin gh-pages"
    },
    "dependencies": {
        "pnpm": "^10.34.1",
        "vitepress": "^1.6.3"
    },
    "pnpm": {
        "overrides": {
            "esbuild": "^0.25.0",
            "vite": "^6.4.2"
        }
    }
}